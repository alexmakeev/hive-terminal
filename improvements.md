# Hive Terminal: Improvements Plan

## Концепция

Превратить низкоуровневый терминал в высокоуровневый интерфейс для работы с AI-агентами и серверами. Автоматизация рутинных операций, умное распознавание контекста.

---

## 1. Smart Paste (Умная вставка)

### 1.1 Распознавание типа контента

| Вставляемое | Действие |
|-------------|----------|
| Путь к файлу `/path/to/file` | Предложить: `cat`, `less`, `nano`, `code` |
| URL `https://...` | Предложить: `curl`, `wget`, открыть в браузере |
| Markdown code block | Автоматически убрать ` ``` ` обёртку |
| JSON/YAML | Форматировать и валидировать перед вставкой |
| Многострочная команда | Показать превью, спросить подтверждение |
| SSH ключ | Предложить добавить в `~/.ssh/authorized_keys` |
| Docker команда | Подсветить, предложить `--rm` если нет |

### 1.2 Контекстные действия

- Вставка в `nano`/`vim` → не показывать диалог, вставить как есть
- Вставка при активном промпте Claude/Aider → форматировать как промпт
- Вставка большого текста (>1000 символов) → предупреждение + опция создать файл

---

## 2. Drag & Drop файлов

### 2.1 Загрузка на сервер

```
[Перетащил файл] → Диалог:
  ┌─────────────────────────────────────┐
  │ Upload config.json to server?       │
  │                                     │
  │ Destination: ~/                     │
  │ Method: ○ SCP  ● SFTP  ○ rsync     │
  │                                     │
  │ [Cancel]              [Upload]      │
  └─────────────────────────────────────┘
```

### 2.2 Скачивание с сервера

- Клик на путь файла в терминале → контекстное меню "Download"
- Выделение пути + горячая клавиша → скачать

### 2.3 Умные подсказки

- `.env` файл → предупреждение о секретах
- Большой файл (>10MB) → предложить сжатие
- Бинарный файл → использовать base64

---

## 3. Clickable Elements (Кликабельные элементы)

### 3.1 Автоматическое распознавание

| Паттерн | Действие по клику |
|---------|-------------------|
| `/absolute/path/file.txt` | Меню: cat, edit, download |
| `./relative/path` | То же + resolve от текущей директории |
| `https://url.com` | Открыть в браузере |
| `user@host:port` | Открыть новый терминал с этим подключением |
| `file.py:42` | Скачать + открыть в редакторе на строке 42 |
| `git@github.com:repo` | Открыть GitHub в браузере |
| `ERROR`, `FAIL`, `Exception` | Выделить красным, предложить поиск |

### 3.2 AI-специфичные паттерны

| Паттерн | Действие |
|---------|----------|
| `Tokens: 1234` | Добавить в трекер расходов |
| `Cost: $0.05` | Агрегировать в статистику сессии |
| `Reading file: ...` | Показать прогресс-бар если много файлов |
| `Wrote file: path` | Подсветить, предложить посмотреть diff |

---

## 4. AI Agent Integration

### 4.1 Автодетект AI CLI

Определять когда запущен:
- Claude Code (`claude`)
- Aider (`aider`)
- GitHub Copilot CLI (`gh copilot`)
- GPT CLI, Gemini CLI, и др.

### 4.2 Enhanced UI для AI режима

```
┌─ Claude Code ──────────────────────────────┐
│ [Status: Thinking...] [Tokens: 12.3K/200K] │
│ [Cost: $0.12] [Context: 45%]              │
├────────────────────────────────────────────┤
│ (обычный терминал)                         │
└────────────────────────────────────────────┘
```

### 4.3 Фичи для AI режима

- **Token counter** — реалтайм подсчёт токенов
- **Cost tracker** — стоимость сессии
- **Context meter** — сколько контекста использовано
- **Pause/Resume** — приостановить агента (Ctrl+Z enhanced)
- **Snapshot** — сохранить состояние разговора

---

## 5. Command Palette & Snippets

### 5.1 Command Palette (Cmd+Shift+P)

```
┌─────────────────────────────────────┐
│ > _                                 │
├─────────────────────────────────────┤
│ 📁 Upload file...                   │
│ 📥 Download file...                 │
│ 🔄 Reconnect                        │
│ ➕ Split horizontal                 │
│ 📋 Copy last output                 │
│ 🔍 Search in output                 │
│ ⚙️  Connection settings             │
└─────────────────────────────────────┘
```

### 5.2 Snippets (Сниппеты)

Пользовательские шаблоны команд:

```yaml
snippets:
  - name: "Docker logs"
    trigger: "dlogs"
    command: "docker logs -f --tail 100 ${1:container}"

  - name: "Find large files"
    trigger: "bigfiles"
    command: "find . -type f -size +${1:100}M"

  - name: "Git sync"
    trigger: "gsync"
    command: "git fetch --all && git pull --rebase"
```

---

## 6. Output Processing

### 6.1 Smart Filtering

- **Live grep** — фильтровать вывод в реальном времени
- **Highlight** — подсветить паттерн без фильтрации
- **Fold** — свернуть повторяющиеся строки (e.g., прогресс-бары)

### 6.2 Output Actions

```
[Правый клик на выводе]
├── Copy
├── Copy as markdown
├── Save to file...
├── Send to another terminal
├── Search Google for error
└── Ask Claude about this
```

### 6.3 Log Mode

Специальный режим для `tail -f`:
- Цветовая схема по уровням (ERROR=red, WARN=yellow)
- Фильтрация по уровню
- Временные метки в локальном времени
- Группировка стектрейсов

---

## 7. Multi-Terminal Features

### 7.1 Broadcast Mode

```
[Broadcast ON] — ввод дублируется во все терминалы
```

Полезно для:
- Обновление нескольких серверов
- Синхронный деплой
- Проверка состояния кластера

### 7.2 Sync Scroll

Синхронная прокрутка для сравнения логов

### 7.3 Terminal Groups

```
Group "Production":
  ├── prod-1 (10.0.0.1)
  ├── prod-2 (10.0.0.2)
  └── prod-3 (10.0.0.3)

[Run on group] → параллельное выполнение
```

---

## 8. Connection Intelligence

### 8.1 Health Monitoring

```
┌─ Connection Status ─────────────────┐
│ ● Latency: 45ms                     │
│ ● Uptime: 2h 34m                    │
│ ● Packets: 0.1% loss                │
│ ● Bandwidth: ~2.3 MB/s              │
└─────────────────────────────────────┘
```

### 8.2 Auto-Reconnect (Smart)

- Exponential backoff
- Показывать "Reconnecting in 5s... [Reconnect now]"
- Сохранять историю команд для replay после реконнекта

### 8.3 SSH Agent Forwarding

- Индикатор статуса SSH agent
- Предупреждение если agent недоступен
- Авто-добавление ключей

---

## 9. Security Features

### 9.1 Secret Detection

Автоматически маскировать в выводе:
- API ключи
- Пароли в connection strings
- AWS credentials
- Private keys

### 9.2 Command Audit

```
[Dangerous command detected]
> rm -rf /var/log/*

Are you sure? This will delete all logs.
[Cancel] [Execute anyway]
```

### 9.3 Session Recording

- Запись сессии для аудита
- Replay mode
- Export в asciinema формат

---

## 10. Workspace Persistence

### 10.1 Layout Save/Restore

```yaml
workspace: "Development"
layout:
  - type: split_h
    children:
      - connection: "dev-server"
        cwd: "/app"
        command: "npm run dev"
      - connection: "dev-server"
        cwd: "/app"
        command: "tail -f logs/app.log"
```

### 10.2 Session Resume

После перезапуска приложения:
- Восстановить layout
- Предложить переподключиться
- Показать последний output (cached)

---

## 11. Integrations

### 11.1 IDE Integration

- VS Code: открыть файл по клику
- JetBrains: то же
- Cursor: интеграция с AI

### 11.2 Cloud Providers

- AWS: `aws ssm start-session` integration
- GCP: `gcloud compute ssh` support
- Kubernetes: `kubectl exec` awareness

### 11.3 Git Integration

- Показывать текущую ветку в статусбаре
- Quick actions: pull, push, status
- Diff preview для изменённых файлов

---

## 12. Mobile-Specific

### 12.1 Gesture Shortcuts

| Жест | Действие |
|------|----------|
| Swipe left | History back |
| Swipe right | History forward |
| Pinch | Font size |
| Long press | Context menu |
| 3-finger tap | Paste |

### 12.2 Quick Actions Bar

```
[Tab] [↑] [↓] [Ctrl] [Esc] [Cmd+C] [Cmd+V]
```

### 12.3 Voice Input

"Run docker ps" → `docker ps`

---

## Приоритизация

### Phase 1 (MVP+)
1. Smart Paste (базовый)
2. Clickable paths/URLs
3. Drag & Drop upload
4. Command palette

### Phase 2 (AI Focus)
5. AI agent detection
6. Token/cost tracking
7. Enhanced AI UI

### Phase 3 (Power Users)
8. Snippets
9. Broadcast mode
10. Workspace persistence

### Phase 4 (Polish)
11. Secret detection
12. Session recording
13. IDE integrations
